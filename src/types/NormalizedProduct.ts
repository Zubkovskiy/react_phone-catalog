export interface NormalizedProduct {
  id: string | number;
  itemId?: string;
  name: string;
  fullPrice: number;
  price: number;
  screen: string;
  capacity: string;
  ram: string;
  image: string;
  category: string;
}
